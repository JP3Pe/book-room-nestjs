{
    "configurations": [
        {
            "command": "source ./env-16.13.1/bin/activate && npx yarn install && npx yarn run prebuild && npx yarn start:dev",
            "name": "NestJS start",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}